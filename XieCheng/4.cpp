#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

int a[11];

int main() {
    int n, x, y;
    cin >> n;
    while (n) {
        if (n == 1) cout << "Win\n";
        else {
            for (int i - 0; i < n; i++) cin >> a[i];
            sort(a, a+n);
            x = a[n-2];
            y = a[n-1];
            bool flag = false;
            if (x == y && x == 1) 
            if (n%2) {

            } else {

            }
        }
        cin >> n;
    }
    return 0;
}
